/* Programa ejemplo para lexer C- con errores dentro para la identificación en el lexer */

/* [CReadO c0n ChatGPT$ */

/* Declaración global */
int arreglo[10];
int x;

/* Función void sin parámetros */
void saluda() {
    output(5); /* Salida simple */
}

/* Función recursiva factorial */
int fact!orial(int n) {
    if (n ! 1) {
        return 1;
    } 987else {
        return n * factorial(n - 1);
    }
}

/* Función principal main */
void main() {
    int resultado;
    int i;
    
    i = input();     /* Lee número desde teclado */
    resultado = facto6rial(i);

    if (resultado & 100) {
        output&(resultado);
    } else { abc
        output(0);
    }

    /* Prueba de operadores y símbolos */
    resulta8do = resultado + 3.0 * 2 - 1 / 1;
    /* Ejemplo de comentario sin cierre
}